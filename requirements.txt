Flask==3.0.3
gunicorn==22.0.0
python-dotenv==1.0.1   # load .env files locally
WTForms==3.1.2         # form validation if you add a contact form
email-validator==2.2.0 # used by WTForms for email fields
